Mesh network with clustered node groups. Node types: servers (computer system boxes), robots (cute, on caterpillars with camera and manipulators), sensors (thermometer icons), storage (pile of disks), and cameras. Each node has a single-line key expression label (e.g. "sensor/room1/temp"). Smaller icons. Inside each cluster, nodes connect to each other (each-to-each). Between clusters, only servers connect to each other. Draw clusters as visually distinct groups with internal mesh connectivity.
