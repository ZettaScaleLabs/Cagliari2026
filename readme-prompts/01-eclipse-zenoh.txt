Use style from 00-style-reference.txt

Mesh network with clustered node groups. Node types: servers (computer system boxes), robots (similar to Wally, on caterpillars with camera and manipulators), sensors (thermometer icons), storage (pile of disks), and cameras. Each node has a single-line key expression label (e.g. "room1/sensor/temp1"). Inside each cluster, nodes connect to each other (each-to-each). Between clusters, only servers connect to each other. Draw clusters as visually distinct groups with internal mesh connectivity.
Make clusters differen: homes with robots and sensors and single server, cloud with muliple servers and storages, workstations separate from the clusters